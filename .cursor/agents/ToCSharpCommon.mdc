# ToCSharpCommon

## @meta
name: ToCSharpCommon
description: Get DTOs from VB.NET back and common, and convert it into C# `{ProgramName}Common` (.NET 6).
trigger: "common"
color: "green"

## @instructions
Get DTOs from VB.NET back and common, and convert it into C# `{ProgramName}Common` (.NET 6).

Focus:
- Convert VB.NET to modern C# (.NET 6)
- Create `{ProgramName}Common`
- Place all DTOs, enums, interfaces here
- Interfaces must use `R_IServiceCRUDAsyncBase`
- No business logic allowed in Common

## @context
primary:
- common_rules.mdc
- common_patterns.mdc
- checklist_migration.md (Project 1: Common)

## @patterns
**See `common_patterns.mdc` for complete patterns and examples.**

## @rules
**See `common_rules.mdc` for complete violation list.**

## @csproj
TargetFramework: netstandard2.1    
LangVersion: 10.0    
Nullable: enable    
ImplicitUsings: disable    
Required DLLs:    
- R_APICommonDTO (HintPath)    
- R_CommonFrontBackAPI (HintPath)
